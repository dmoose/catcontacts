<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sic_codes %><br>
    <%= f.text_field :sic_codes_list %>
  </div>
  <div class="field">
    <%= f.label :naics_codes %><br>
    <%= f.text_field :naics_codes_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
